[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = test_*.py
addopts = --cov=accounts --cov=engagements --cov=llm --cov=audit --cov=pmo_agent --cov-report=term-missing
markers =
    unit: 外部依存(DB/HTTP)を持たない純粋ロジックの単体テスト
    integration: DBやモックHTTPなど外部依存を伴う統合テスト
